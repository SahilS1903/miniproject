name: bot_detection_app
description: A Flutter app for bot detection using machine learning modules

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.2.6
  google_fonts: ^4.0.3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  file_picker: ^6.0.0  # Ensure it's at the root level, not inside `flutter:`
